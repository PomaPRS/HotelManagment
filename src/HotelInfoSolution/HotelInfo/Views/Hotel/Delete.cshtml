@model HotelInfo.Models.HotelViewModel

@{
    ViewBag.Title = "Delete";
}

<h3>Подтвердите удаление гостиницы</h3>

<div class="panel panel-default">
    <div class="panel-body">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.IndividualId)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.IndividualId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Address)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Address)
            </dd>
        </dl>

        <dl class="dl-horizontal">
            <dd>
                @using (Html.BeginForm("DeleteConfirm", "Hotel", FormMethod.Post))
                {
                    @Html.HiddenFor(x => x.Id)
                    <div class="form-actions no-color">
                        <input type="submit" value="Удалить" class="btn btn-primary" />
                    </div>
                }
            </dd>
        </dl>
    </div>
</div>

<div>
    @Html.ActionLink("Назад", "Index")
</div>